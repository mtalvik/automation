<!DOCTYPE html>
<html lang="et">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ vault_website_title }}</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            min-height: 100vh;
        }
        .container {
            background: rgba(255,255,255,0.1);
            padding: 30px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px rgba(0,0,0,0.2);
        }
        h1 {
            text-align: center;
            margin-bottom: 30px;
            font-size: 2.5em;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        .card {
            background: rgba(255,255,255,0.2);
            border-radius: 10px;
            padding: 20px;
            border: 1px solid rgba(255,255,255,0.3);
        }
        .card h3 {
            margin-bottom: 15px;
            color: #FFD700;
            border-bottom: 2px solid #FFD700;
            padding-bottom: 5px;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            margin: 10px 0;
            padding: 8px;
            background: rgba(255,255,255,0.1);
            border-radius: 5px;
        }
        .info-label {
            font-weight: bold;
        }
        .info-value {
            color: #90EE90;
        }
        .secret-card {
            background: rgba(255, 193, 7, 0.3);
            border: 1px solid rgba(255, 193, 7, 0.5);
        }
        .vault-info {
            background: rgba(40, 167, 69, 0.3);
            border: 1px solid rgba(40, 167, 69, 0.5);
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            opacity: 0.8;
            border-top: 1px solid rgba(255,255,255,0.3);
            padding-top: 20px;
        }
        .badge {
            display: inline-block;
            padding: 4px 8px;
            background: rgba(0,123,255,0.8);
            border-radius: 12px;
            font-size: 0.8em;
            margin-left: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🔐 {{ vault_website_title }}</h1>
        
        <div class="grid">
            <!-- Server Information Card -->
            <div class="card">
                <h3>🖥️ Server Information</h3>
                <div class="info-row">
                    <span class="info-label">Server Name:</span>
                    <span class="info-value">{{ server_name }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Admin Email:</span>
                    <span class="info-value">{{ admin_email }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Generated:</span>
                    <span class="info-value">{{ ansible_date_time.iso8601 }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Operating System:</span>
                    <span class="info-value">{{ ansible_os_family }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Hostname:</span>
                    <span class="info-value">{{ ansible_hostname }}</span>
                </div>
            </div>

            <!-- Vault Secrets Card -->
            <div class="card secret-card">
                <h3>🔐 Vault Secrets (Secure)</h3>
                <div class="info-row">
                    <span class="info-label">Student:</span>
                    <span class="info-value">{{ vault_student_name }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">MySQL Password Length:</span>
                    <span class="info-value">{{ vault_mysql_password | length }} characters</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Admin Password Length:</span>
                    <span class="info-value">{{ vault_admin_password | length }} characters</span>
                </div>
                <div class="info-row">
                    <span class="info-label">API Key Length:</span>
                    <span class="info-value">{{ vault_api_key | length }} characters</span>
                </div>
                <p style="margin-top: 15px; font-style: italic; opacity: 0.9;">
                    🛡️ Full passwords are safely encrypted in Ansible Vault!
                </p>
            </div>

            <!-- System Status Card -->
            <div class="card vault-info">
                <h3>⚡ System Status</h3>
                <div class="info-row">
                    <span class="info-label">Memory:</span>
                    {% if ansible_memtotal_mb > 2048 %}
                    <span class="info-value">{{ ansible_memtotal_mb }}MB <span class="badge">High Memory</span></span>
                    {% else %}
                    <span class="info-value">{{ ansible_memtotal_mb }}MB <span class="badge">Standard</span></span>
                    {% endif %}
                </div>
                <div class="info-row">
                    <span class="info-label">Architecture:</span>
                    <span class="info-value">{{ ansible_architecture }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Processor Count:</span>
                    <span class="info-value">{{ ansible_processor_count }}</span>
                </div>
            </div>

            <!-- Network Information Card -->
            <div class="card">
                <h3>🌐 Network Interfaces</h3>
                {% for interface in ansible_interfaces %}
                    {% if interface != 'lo' %}
                    <div class="info-row">
                        <span class="info-label">{{ interface }}:</span>
                        <span class="info-value">
                            {% if ansible_default_ipv4.address is defined %}
                                {{ ansible_default_ipv4.address }}
                            {% else %}
                                No IP assigned
                            {% endif %}
                        </span>
                    </div>
                    {% endif %}
                {% endfor %}
            </div>
        </div>

        <div class="footer">
            <p>🎓 <strong>ITS-24 DevOps Automation Course</strong></p>
            <p>Week 13 - Ansible Vault & Templates Homework</p>
            <p>Generated by Ansible template system on {{ ansible_date_time.date }}</p>
            <p style="margin-top: 10px; font-size: 0.9em;">
                Template demonstrates: Vault variables, Jinja2 loops, conditionals, and filters
            </p>
        </div>
    </div>
</body>
</html>
