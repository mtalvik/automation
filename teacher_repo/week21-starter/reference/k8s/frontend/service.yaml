apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: week21-app
  labels:
    app: frontend
    component: frontend
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    name: http
  type: LoadBalancer
